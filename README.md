## Africa XYZ Challenge | React Native

### Getting Started

- Clone the repo : `git clone https://github.com/dushimeemma/africe-xyz-challenge-rn`
- Install dependencies: `yarn install`
- Run the app : `expo start`
